<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
    </style>

    <style name="ClubToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item><!-- 设置该属性解决空白部分-->
    </style>

    <style name="baseTabStyle" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#FF000000</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabSelectedTextColor">#FF000000</item>
        <item name="tabTextAppearance">@style/baseTabTextAppearance</item>
    </style>
    <style name="baseTabTextAppearance" parent="TextAppearance.Design.Tab">  <!--TAB文字样式-->

        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#FF666666</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>    <!--英文大写,默认true-->
    </style>
</resources>
